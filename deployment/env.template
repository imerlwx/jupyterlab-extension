# Environment Variables for JupyterHub Extension
# Copy this file to /etc/jupyterhub/env and fill in your actual API keys
#
# IMPORTANT: Keep this file secure and never commit it to git
# Set permissions: sudo chmod 600 /etc/jupyterhub/env

# OpenAI API Key (required for AI chat functionality)
# Get your key at: https://platform.openai.com/api-keys
export OPENAI_API_KEY="sk-your-openai-api-key-here"

# YouTube Data API Key (required for video transcript fetching)
# Get your key at: https://console.cloud.google.com/apis/credentials
export YOUTUBE_API_KEY="your-youtube-api-key-here"

# JupyterHub Proxy Authentication Token (optional - will be auto-generated if not set)
# Generate a random token with: openssl rand -hex 32
# export CONFIGPROXY_AUTH_TOKEN="your-random-token-here"

# Firebase Configuration (for logging user interactions and learning analytics)
# Get your Firebase credentials from: https://console.firebase.google.com/
# 1. Create a Firebase project
# 2. Go to Project Settings > Service Accounts
# 3. Click "Generate New Private Key" and download the JSON file
# 4. Place the JSON file in /etc/jupyterhub/ (e.g., /etc/jupyterhub/firebase-credentials.json)
# 5. Set the path and database URL below:
export FIREBASE_CREDENTIALS_PATH="/etc/jupyterhub/firebase-credentials.json"
export FIREBASE_DATABASE_URL="https://your-project-id.firebaseio.com"

# Additional environment variables for your extension (if needed)
# export CUSTOM_VAR="value"
